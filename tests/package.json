{
  "name": "mocha",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "expect.js": "^0.3.1",
    "fetch-cookie": "^0.10.1",
    "graphql": "^15.3.0",
    "graphql-request": "^3.2.0",
    "mocha": "^8.2.0",
    "mocha-steps": "^1.3.0"
  },
  "scripts": {
    "test": "yarn mocha -r mocha-steps -t 5000 scenarios/*.js",
    "watch": "yarn mocha -r mocha-steps -t 5000 --watch scenarios/*.js"
  }
}
